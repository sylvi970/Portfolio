[build]
  command = "flutter build web"
  publish = "build/web"

[[redirects]]
  # Redirect all routes to index.html for a single-page application
  from = "/web/*"
  to = "/index.html"
  status = 200
